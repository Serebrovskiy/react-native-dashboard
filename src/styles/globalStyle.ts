import { StyleSheet } from 'react-native';

export const globalStyle = StyleSheet.create({
    main: {
        flex: 1,
        padding: 20,
        paddingTop: 60,
        backgroundColor: '#fff', 
        color: '#060814',
    },
})